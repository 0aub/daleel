name: daleel

services:
  daleel:
    build: .
    container_name: daleel
    env_file:
      - path: .env
        required: false
    volumes:
      - .:/app
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app/src
    entrypoint: ["python", "src/main.py"]
